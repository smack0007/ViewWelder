<Window x:Class="HelloWorld.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloWorld.Views"
        xmlns:vw="clr-namespace:ViewWelder;assembly=ViewWelder"
        mc:Ignorable="d"
        Title="ShellView" Width="800" Height="600">
    <Grid>
        <TabControl ItemsSource="{Binding People}" SelectedItem="{Binding SelectedPerson}">
            <TabControl.ContentTemplate>
                <vw:ResolveViewDataTemplate Path="." Mode="TwoWay" />
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
